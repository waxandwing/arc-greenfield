Kelly desk asset drop folder
==============================

************************************************************************
** KELLY: PUT THE 22 LABELED SVGs IN THIS FOLDER — uploads/desk-incoming/
** See KELLY-DROP-22-LABELED-SVGS-HERE.txt + LABELED-SVG-CHECKLIST.md
************************************************************************

**PRIMARY DROP PATH FOR LABELED SVGs:** this folder — `uploads/desk-incoming/`

Put new desk PNGs, SVGs, or exports here when you are not sure of the final folder.
Git is set up to track files in this folder (PNG and SVG are not ignored).

Steps to get your files onto GitHub so an agent can use them:

  1. Drag your PNG or SVG files into this folder (uploads/desk-incoming/) in Cursor or Finder.
  2. Edit this README and add one line per file under "Files in this folder" — filename plus what the image is.
  3. In Terminal, from the repo root: git add uploads/desk-incoming/
  4. Commit: git commit -m "Add Kelly desk assets to incoming drop folder"
  5. Push: git push origin main
  6. In Cursor, ask: "Incorporate Kelly labeled SVGs from uploads/desk-incoming"

An agent will move files into public/assets/desk/, public/assets/desk/slices/,
public/assets/desk/figma/, public/assets/desk/canonical/, public/assets/arc/icarus/,
or public/assets/arctable/ as appropriate. Do not put images in src/.

Example line for the list below:
  new-priorities-tab.png — folder tab from Figma export 2026-09-15

Files in this folder (update this list when you add assets):

  KELLY-DROP-22-LABELED-SVGS-HERE.txt — plain-English drop instructions (READ FIRST)
  LABELED-SVG-CHECKLIST.md — exact 22 filenames + wiring order
  kelly-desk-v3-preview-repair-2026-08-03.png — CURRENT APP DISPLAY baseline only (not visual target)

Teaching week zip (Teaching_week__1__01fd.zip) — extracted to teaching-week-1/:
  Only 1.png (full comp) + 2.png (blank). Comp crops are opt-in only.
  The labeled SVG pack is SEPARATE from this zip — drop the 22 SVGs here.

========================================================================
BLOCKED — Kelly labeled SVG pack (22 files) — drop HERE
========================================================================

Re-audit 2026-09-16: visual grade improved RED → YELLOW/RED, but the
22 labeled source SVGs are STILL MISSING. That is the major blocker.

Already landed (do NOT treat as current fails):
  Teaching week title, week kicker, rainbow mark, 3-course week grid.

Still blocked until you drop binaries:
  Wood / tray / Settings+TO-DOS tabs / calendar tabs / calendar bg /
  rainbow SVG / today / start-class / magnets — then pixel gate.

Planning Period audit (same day): Kelly selected .arc-desk-surface — photographic
TO-DOS strip, IDEAS green pill, SETTINGS, edge-tab PNG crops, wood, planner buckets
all still blocked. See LABELED-SVG-CHECKLIST.md § Planning Period view. No wires /
CSS fakes until binaries land.

Exact expected filenames: LABELED-SVG-CHECKLIST.md
Highest priority (USE) for Planning Period + Teaching week:

  - TODO tab (use).svg
  - tray image (use).svg
  - Settings tab (USE).svg
  - calendar selected tab view.svg / unselected calendar states.svg
  - Wood Background Light.svg
  - calendar background.svg

Also needed for full P0 pack:

  - start class arctable logo.svg
  - blue/green/yellow/red magnet SVGs
  - to left of date on calendar rainbow icon.svg
  - today button.svg / today highlighter icon.svg

Preserve exact names (incl. "(USE)"). A single zip is fine.
Agents rename into public/assets/desk/canonical/ and wire in order —
they must NOT invent fake Kelly art or prettier CSS on wrong assets.
