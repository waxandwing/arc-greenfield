# CI verification marker

Temporary documentation-only marker used to trigger the existing `feature/**` verification workflow against the architecture verification tree. This branch is not a merge or deployment candidate.
