# Photoroom pattern exports (local)

Extract of Kelly's `Photoroom_da1f.zip` for audit. **Not production.**

```bash
unzip -o uploads/Photoroom_da1f.zip -d .local/photoroom-patterns/
```

Evidence copies: `docs/overnight/evidence/asset-reconciliation/photoroom-patterns/`
