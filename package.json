{
  "name": "arc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test:calendar": "tsc -p tsconfig.calendar-test.json && node tests/generated/tests/calendar.contract.js && node tests/generated/src/calendar/projections.contract.js",
    "build": "npm run test:calendar && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "5.8.3",
    "vite": "latest"
  }
}
