#!/usr/bin/env node
console.log(
  '[preview:desk] If port 4173 is already in use, run `npm run preview:desk:stop` or open the existing server:',
)
console.log('  http://127.0.0.1:4173/?demo=1&demoReset=1')
